import { FilmPageLoader } from "@/components/shared/FilmPageLoader";

export default function RegisterLoading() {
  return (
    <FilmPageLoader
      text="Loading registration..."
      subtext="Join FilmRank to track, rate & discover movies"
    />
  );
}
